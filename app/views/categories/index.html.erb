<h1>Soup Categories</h1>
<ul>
  <% @categories.each do |category| %>
      <p><li><strong></strong><%= link_to category.name, category %> (<%= category.soups.count %>)</li></p>
  <% end %>
  <% if session[:vip] %>
      <li>Kale Based</li>
  <% end %>
</ul>